// Challenge prototype UI removed — if you need it back, we can restore it later.
import 'package:flutter/material.dart';

class ChallengePrototypePagePlaceholder extends StatelessWidget {
  const ChallengePrototypePagePlaceholder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reto - Protótipo (removido)')),
      body: const Center(child: Text('La página de prototipo del reto ha sido removida.')),
    );
  }
}
