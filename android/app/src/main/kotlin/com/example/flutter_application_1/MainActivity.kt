package com.example.flutter_application_1

// MainActivity removed FlutterActivity reference because the manifest
// now uses the framework embedding activity directly.
// Keep an empty placeholder class to avoid build errors from missing file.
class MainActivity
